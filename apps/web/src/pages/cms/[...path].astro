---
export const prerender = false;
// Serve the static CMS file
const response = await fetch(new URL("/cms/index.html", Astro.url.origin));
const html = await response.text();
return new Response(html, {
  status: 200,
  headers: {
    "Content-Type": "text/html",
  },
});
---

